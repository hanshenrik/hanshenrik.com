---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const { href, class: className, ...rest } = Astro.props;
---

<a href={href} class:list={["wave-link", className]} {...rest}><slot /></a>
