---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  noWave?: boolean;
}

const { href, class: className, noWave = false, ...rest } = Astro.props;
---

<a href={href} class:list={[noWave ? "" : "wave-link", className]} {...rest}
  ><slot /></a
>
